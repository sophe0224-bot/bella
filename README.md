# Labubu Chatbox

A delightfully loud welcome screen starring five adorable, animated Labubus.

## Run locally

```bash
pnpm install
pnpm dev
```

Use `pnpm build` for a production build and `pnpm test` to run the rendered-page checks.
